events {}
http {
    upstream backend {
       {% for ser in groups.webservers %}
         server {{ser}};
       {% endfor %}
    }
 server {
   listen 80;
   location / {
     proxy_pass  http://backend;
   }
 }
}
